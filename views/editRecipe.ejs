<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title>My Meal Prep</title>
        <!-- Favicon-->
        <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
        <!-- Core theme CSS (includes Bootstrap)-->
        <link href="css/styles.css" rel="stylesheet" />
    </head>
    <body>
        <!-- Responsive navbar-->
        <nav class="navbar navbar-dark bg-dark">
            <!-- Wrap navbar in a container -->
            <div class="container">
                <!-- Nav Title/Logo -->
                <div class="nav-header">
                    <a class="navbar-brand" href="#">DWymer</a>
                </div>
                <!-- Main Nav links -->
                <div class="nav-links navbar-expand-lg">
                    <ul class="navbar-nav">
                        <li class="nav-item"><a class="nav-link active" aria-current="page" href="/">Home</a></li>
                        <li class="nav-item"><a class="nav-link" href="/food">Food</a></li>
                        <li class="nav-item"><a class="nav-link" href="/recipes">Recipes</a></li>
                    </ul>
                </div>
                <!-- User specific nav links -->
                <div class="nav-user navbar-expand-lg">
                    <ul class="navbar-nav">
                        <li class="nav-item"><a class="nav-link" href="/profile">Profile</a></li>
                        <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
                        <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
                    </ul>
                </div>
            </div>
        </nav>
        <!-- Page content-->
        <div class="container">
            <div class="recipeDetails text-center mt-4">
            </div>
            <div class="editRecipe">
                <h1 class="text-center">Edit Recipe</h1>
                <!-- on submit(pressing createIngredient), load /createIngredient and post the ingredient data -->
                <form action="/createRecipe" method="post">
                    <!-- row for: Recipe Name/Type and Serving Size - Each input will be grouped seperatly -->
                    <div class="form-group row mt-4">
                        <div class="form-group col-6">
                            <label for="name">Recipe Name:</label>
                            <input class="form-control" type="text" name="name" value="<%= recipe.name %>">
                        </div>
                        <div class="form-group col-4">
                            <label for="type">Recipe Type:</label>
                            <input class="form-control" type="text" name="type" value="<%= recipe.type %>"">
                        </div>
                        <div class="form-group col-2">
                            <label for="serves">Serves:</label>
                            <input class="form-control" type="number" name="serves" value="<%= recipe.serves %>">
                        </div>
                    </div>
                    <div class="form-group mt-4">
                        <label for="description">Recipe Description:</label>
                        <textarea class="form-control" type="text" name="description" rows="5"><%= recipe.description %></textArea>
                    </div>
                    <div class="form-group mt-4">
                        <label for="recipeImage">Add Image (currently not used)</label>
                        <input type="file" class="form-control-file" id="recipeImage">
                    </div>

                    <h1 class="text-center mt-5">Edit Recipe Ingredients</h1>
                    <!--  Created a href to send through the id of the selected Recipe to the url to use in the SelecetedFood page  -->
                    <table class="table" id="recipeIngredient-list">
                        <tr>
                            <a href="/food/recipe=<%= recipe.id %>">Add Ingredient</a>
                        </tr>
                        <thead>
                            <td>recipe id: </td>
                            <td>ingredient id: </td>
                            <td>amount: </td>
                            <td>weight: </td>
                            
                        </thead>
                        <% if (typeof allRecipeIngredients !== 'undefined') { %>
                            <% allRecipeIngredients.forEach((recipeIngredient) => { %>
                        <tr> 
                            <td><%= recipeIngredient.recipeid %></td>
                            <td><%= recipeIngredient.ingredientid%></td>
                            <td><%= recipeIngredient.amount %></td>
                            <td><%= recipeIngredient.measurement %></td>
                            <!-- passing through the recipe id so that once the delete has been done the app knows what recipe to return to -->
                            <td><a class="btnAddIngredient" href="/viewRecipeIngredient/recipe=<%= recipeIngredient.recipeid %>&recipeIngredient=<%= recipeIngredient.recipeIngredientid %>" >Edit Ingredient:</a></td>
                            <!-- passing through the recipe id so that once the delete has been done the app knows what recipe to return to -->
                            <td><a class="btnAddIngredient" href="/removeIngredient/recipe=<%= recipeIngredient.recipeid %>&recipeIngredient=<%= recipeIngredient.recipeIngredientid %>" >Remove Ingredient:</a></td>
                        </tr>
                        <% }); %>
                    </table>
                    <% } %>
                    <h1 class="text-center mt-5">Edit Recipe Instructions</h1>
                    <input class="btn btn-primary" type="submit" value="Add Instruction">
                    <!-- Submit Button for Creating the Base of a Recupe (added button into a div to help center) -->
                    <div class="button text-center mt-5">
                        <input class="btn btn-primary" type="submit" value="Edit Recipe">
                    </div>
                </form>
            </div> 
        
        </div>
        <!-- Bootstrap CSS only-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
        <!-- Core theme JS-->
        <script src="js/scripts.js"></script>
    </body>
</html>